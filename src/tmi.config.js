const config = {
  options: {
    debug: false
  },
  connection: {
    reconnect: true
  },
  identity: {
    username: 'emoteanalyzer',
    password: '' // need auth token
  },
  channels : []
};

export default config;